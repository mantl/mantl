{
    "check": {
        "id": "distributive-logrotate-checks",
        "name": "Distributive logrotate Health Checks",
        "script": "/bin/distributive -f /etc/distributive.d/logrotate.json -d '' --verbosity=info",
        "status": "passing",
        "interval": "1m",
        "timeout": "30s"
    }
}
