{
    "check": {
        "id": "distributive-mantlui-checks",
        "name": "Distributive mantlui Health Checks",
        "script": "/bin/distributive -f /etc/distributive.d/mantlui.json -d '' --verbosity=info",
        "interval": "1m",
        "timeout": "30s"
    }
}
