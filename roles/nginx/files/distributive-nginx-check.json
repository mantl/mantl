{
    "check": {
        "id": "distributive-nginx-checks",
        "name": "Distributive nginx Health Checks",
        "script": "/bin/distributive -f /etc/distributive.d/nginx.json -d '' --verbosity=info",
        "status": "passing",
        "interval": "1m",
        "timeout": "30s"
    }
}
